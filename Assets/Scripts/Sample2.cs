using UnityEngine;

public class Sample2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Sample2에 대한 기능 테스트");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
